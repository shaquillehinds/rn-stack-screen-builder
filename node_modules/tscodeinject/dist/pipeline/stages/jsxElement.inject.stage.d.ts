import { Collection, JSCodeshift } from "jscodeshift";
import { StageOptionsAndIdName } from "../../@types/stage.js";
/**
 *
 * @param options.jsxElementTemplate - Provide jsx element signatures in a template string
 * @example
 * ```
 * (`{
 *  template: true
 * }`)
 * ```
 */
export default function injectJSXElementStage(jcs: JSCodeshift, workingSource: Collection, { stringTemplate, col }: StageOptionsAndIdName<"jsxElement">): Collection;
