import { Collection, JSCodeshift } from "jscodeshift";
import { StageOptionsAndIdName } from "../../@types/stage.js";
/**
 *
 * @param options.trueClause - Provide type signature in a template string
 * @example
 * ```
 * (`
 *   CustomType | "someAlias"
 * `)
 * ```
 */
export default function injectTSTypeAliasConditionalStage<T extends "tsTypeAliasConditional">(jcs: JSCodeshift, workingSource: Collection, opts: StageOptionsAndIdName<T>): Collection;
