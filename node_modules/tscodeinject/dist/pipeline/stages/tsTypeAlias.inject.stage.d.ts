import { Collection, JSCodeshift } from "jscodeshift";
import { StageOptionsAndIdName } from "../../@types/stage.js";
/**
 *
 * @param options.stringTemplate - Provide type signature in a template string
 * @example
 * ```
 * (`
 *   CustomType | "someAlias"
 * `)
 * ```
 */
export default function injectTSTypeAliasStage<T extends "tsTypeAlias">(jcs: JSCodeshift, workingSource: Collection, options: StageOptionsAndIdName<T>): Collection;
