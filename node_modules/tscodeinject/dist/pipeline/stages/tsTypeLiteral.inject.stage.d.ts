import { Collection, JSCodeshift } from "jscodeshift";
import { StageOptionsAndIdName } from "../../@types/stage.js";
/**
 *
 * @param options.stringTemplate - Provide type literal signatures in a template string
 * @example
 * ```
 * (`{
 *  template: true
 * }`)
 * ```
 */
export default function injectTSTypeLiteralStage(jcs: JSCodeshift, workingSource: Collection, { stringTemplate, col, idName, forceInject }: StageOptionsAndIdName<"tsTypeLiteral">): Collection;
