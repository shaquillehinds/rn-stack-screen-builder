import { Collection, JSCodeshift } from "jscodeshift";
import { StageOptions } from "../../@types/stage.js";
/**
 *
 * @param options.stringTemplate - Provide typescipt code in a template string
 * @example
 * ```
 * (`
 * function hello(){
 *    return "world";
 *  }
 * `)
 * ```
 */
export default function injectStringTemplateStage(jcs: JSCodeshift, workingSource: Collection, { template, position, col }: StageOptions<"stringTemplate">): Collection;
