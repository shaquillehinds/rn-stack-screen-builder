import { Collection, JSCodeshift } from "jscodeshift";
import { StageOptionsAndIdName } from "../../@types/stage.js";
/**
 *
 * @param options.bodyStringTemplate - Provide interface signatures in a template string
 * @example
 * ```
 * (`{
 *  template: true
 * }`)
 * ```
 */
export default function injectTSInterfaceBodyStage(jcs: JSCodeshift, workingSource: Collection, { bodyStringTemplate, col }: StageOptionsAndIdName<"tsInterfaceBody">): Collection;
