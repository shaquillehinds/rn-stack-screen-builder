import singleNodeCollectionValidator from "../validators/singleNodeCollection.finder.validator.js";
export default function arrayVariableFinder(jcs, collection, { name }) {
    return singleNodeCollectionValidator(collection
        .find(jcs.Identifier, { name })
        .closest(jcs.VariableDeclaration)
        .find(jcs.ArrayExpression), name);
}
