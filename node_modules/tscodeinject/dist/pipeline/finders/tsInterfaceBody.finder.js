import singleNodeCollectionValidator from "../validators/singleNodeCollection.finder.validator.js";
export default function tsInterfaceBodyFinder(jcs, collection, { name }) {
    return singleNodeCollectionValidator(collection
        .find(jcs.TSInterfaceDeclaration, { id: { type: "Identifier", name } })
        .find(jcs.TSInterfaceBody), name);
}
