import emptyCollectionValidator from "../validators/emptyCollection.finder.validator.js";
export default function programFinder(jcs, collection) {
    return emptyCollectionValidator(collection.find(jcs.Program), "Program");
}
