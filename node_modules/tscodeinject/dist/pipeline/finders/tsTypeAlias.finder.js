import optionalNodeCollectionValidator from "../validators/optionalNodeCollection.finder.validator.js";
export default function tsTypeAliasFinder(jcs, collection, { name }) {
    return optionalNodeCollectionValidator(collection.find(jcs.TSTypeAliasDeclaration, {
        id: { type: "Identifier", name }
    }), name);
}
