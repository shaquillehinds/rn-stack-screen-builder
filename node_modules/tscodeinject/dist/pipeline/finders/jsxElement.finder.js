import optionalNodeCollectionValidator from "../validators/optionalNodeCollection.finder.validator.js";
export default function jsxElementFinder(jcs, collection, { name }) {
    const [object, property] = name.split(".");
    return optionalNodeCollectionValidator(collection.find(jcs.JSXElement, {
        openingElement: property
            ? {
                name: {
                    type: "JSXMemberExpression",
                    property: { type: "JSXIdentifier", name: property },
                    object: { type: "JSXIdentifier", name: object }
                }
            }
            : { name: { type: "JSXIdentifier", name: object } }
    }), name);
}
