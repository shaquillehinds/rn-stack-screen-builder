import { Collection, JSCodeshift } from "jscodeshift";
/**
 * Finds undeclared export
 *
 * @example
 * ```
 * export {
 *   example
 * }
 * ```
 */
export default function exportFinder<T>(jcs: JSCodeshift, collection: Collection<T>): {
    idName: string;
    col: import("jscodeshift/src/Collection").Collection<import("ast-types").namedTypes.ExportNamedDeclaration>;
};
