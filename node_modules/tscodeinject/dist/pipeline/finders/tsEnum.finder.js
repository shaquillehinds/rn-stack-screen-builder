import optionalNodeCollectionValidator from "../validators/optionalNodeCollection.finder.validator.js";
export default function tsEnumFinder(jcs, collection, { name }) {
    return optionalNodeCollectionValidator(collection.find(jcs.TSEnumDeclaration, {
        id: { type: "Identifier", name }
    }), name);
}
