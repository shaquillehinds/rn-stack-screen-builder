import arrToExp from "./arrToExp.parser.js";
import objToExp from "./objToExp.parser.js";
import funcToExp from "./funcToExp.parser.js";
export default function anyToExp(jcs, value) {
    switch (typeof value) {
        case "object":
            if (value instanceof Array)
                return arrToExp(jcs, value);
            else
                return objToExp(jcs, value);
        case "function":
            return funcToExp(jcs, value);
        default:
            return jcs.literal(value);
    }
}
