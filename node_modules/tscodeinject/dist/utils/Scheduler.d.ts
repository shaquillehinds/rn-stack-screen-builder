/// <reference types="node" />
/// <reference types="node" />
import { EventEmitter } from "events";
export declare class Schedule extends EventEmitter {
    handle?: NodeJS.Timer;
    action: (schedule: Schedule) => void;
    time: number;
    constructor(action: (schedule: Schedule) => void, time: number);
    start(): void;
    stop(): void;
}
export declare class Timer extends EventEmitter {
    handle?: NodeJS.Timer;
    action: () => void;
    time: number;
    constructor(action: () => void, time: number);
    start(): void;
    stop(): void;
}
declare const _default: {
    Timer: typeof Timer;
    Schedule: typeof Schedule;
};
export default _default;
