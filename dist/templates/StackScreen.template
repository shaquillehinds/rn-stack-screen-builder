import React from 'react';
import { StyleSheet, View } from 'react-native';
import {{ScreenName}}Controller from './{{ScreenName}}.controller';

export default function {{ScreenName}}Screen(
  props: {{NavigatorName}}StackScreenProps<'{{ScreenName}}'>,
) {
  const controller = {{ScreenName}}Controller(props);
  return (<View></View>);
}

const styles = StyleSheet.create({});
